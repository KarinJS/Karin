{
  "name": "node-karin-cli",
  "version": "1.0.0",
  "description": "node-karin cli",
  "keywords": [],
  "homepage": "https://github.com/KarinJS/Karin",
  "bugs": {
    "url": "https://github.com/KarinJS/Karin/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KarinJS/Karin.git"
  },
  "license": "MIT",
  "author": "shijin",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "app": "node dist/index.mjs",
    "cp": "node index.mjs",
    "dev": "tsx src/index.ts",
    "build": "pnpm cp && tsc && vite build --configLoader native && tsdown",
    "build:core": "pnpm cp && tsc && vite build --mode core --configLoader native && tsdown --config tsdown.core.config.ts",
    "build:dev": "pnpm cp && tsc && vite build --mode development --configLoader native && tsdown"
  },
  "devDependencies": {
    "commander": "^13.1.0",
    "tsdown": "^0.22.2"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "time": "2025-04-10T19:55:45.123Z",
  "dependencies": {
    "yaml": "2.7.0"
  }
}
