const key = {
  token: 'token',
  theme: 'theme',
  sideBarCollapsed: 'side-bar-collapsed',
  userId: 'userId',
  accessToken: 'accessToken',
  refreshToken: 'refreshToken'
} as const

export default key
